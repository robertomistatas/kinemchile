"use client"

import * as React from "react"
import { ChevronLeft, ChevronRight } from "lucide-react"
// Componente placeholder seguro para evitar errores de importación
export function Calendar() {
  return <div>Componente de calendario migrado. Usa react-datepicker en su lugar.</div>;
}
